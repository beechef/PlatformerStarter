using System;
using UnityEngine;

[RequireComponent(typeof(StatsController))]
public class CollectionController : MonoBehaviour
{

    public virtual void Heal(float health)
    {
     
    }

    public virtual void AttackSpeed(float attackSpeed)
    {
        
    }
}